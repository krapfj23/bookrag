# Slice 2 Review — upload-pipeline-status

**Date:** 2026-04-21
**Verdict:** APPROVE
**Reviewer:** Evaluator agent (superpowers:code-reviewer)

## Rubric check

1. **Tests: PASS** — `pytest tests/ --tb=no -q` → `906 passed, 5 warnings in 18.09s`. `npm test -- --run` → `Test Files 12 passed (12), Tests 56 passed (56)`. `npm run test:e2e` → `4 passed (2.6s)`. All three counts match the expected values in the rubric (pytest 906, Vitest ≥ 56, Playwright 4/4). Only warnings are pre-existing numpy/coroutine warnings and an `act()` advisory from a React state update that is legitimate (scheduled by the polling timer mid-teardown); no test regresses.
2. **Dev server: PASS** — Backend `python main.py` comes up cleanly: `curl localhost:8000/health` → `{"status":"ok","version":"0.1.0"}`; `curl localhost:8000/books` → `[{"book_id":"christmas_carol_e6ddcd76",...}]`; no exceptions in log tail (only the pre-existing "Skipping …: pipeline_state.json missing" INFO lines from stale fixture dirs). Frontend `npm run dev` reports `VITE v5.4.21 ready in 131 ms` within 5 seconds with no compile errors. Visual confirmation of the upload screen is provided indirectly via the four green Playwright tests, which drive the real Vite dev server through `webServer` and assert on real DOM (`heading "Upload an EPUB."`, all seven stage labels, the `role="alert"` copy, and full navigation round-trips).
3. **Config drift: PASS** — `git diff 6e5ff99..HEAD` touches zero backend files (`main.py`, `models/config.py`, `config.yaml`, `.env*` all unchanged). CORS still allows `http://localhost:5173` via the same `_CORS_ORIGINS` list (`main.py:87-98`). Only frontend dependencies (`react-router-dom@6.30.3`, `@playwright/test@1.59.1`) are added to `frontend/package.json`; no new env vars are introduced anywhere. `BASE_URL` is hard-coded to `http://localhost:8000` in `frontend/src/lib/api.ts:51`, matching slice-1 convention.
4. **Acceptance criteria: PASS** — all 10 criteria are exercised in code and at least one test. See per-criterion table below.
5. **Scope: PASS** — the diff stays entirely within the "NEW in this slice" list: Dropzone, StatusBadge, PipelineRow, icons (IcUpload/IcCheck/IcClose), UploadScreen, `uploadBook`/`fetchStatus` in `lib/api.ts`, router wiring in `main.tsx`/`App.tsx`, and a small NavBar update to drive `active` from `useLocation()`. No reading-screen, chat, SSE, dark-mode, or optimistic-library code appears. No backend endpoints were added. The Generator's flagged deviations (`0.4s` sub-second formatting and folding `.gitignore` housekeeping into the Task 7 commit) are both out-of-scope for REVISE per the rubric instructions.

## Per-criterion verification

1. **"Clicking the Upload tab in the NavBar navigates to /upload and renders the Upload screen; clicking Library returns to / and re-renders the Library. Neither reloads the page. The Reading tab remains inert…"** — PASS. `NavBar.tsx:82-91` renders `<Link to="/">` and `<Link to="/upload">`; Reading is rendered as `<a href="#" aria-disabled="true" onClick={e => e.preventDefault()}>` (`NavBar.tsx:67-78`). E2E `upload.spec.ts:75-87` drives the full Library → Upload → Library click path with `expect(page).toHaveURL(/\/upload$/)` and `.../\/$/`. `NavBar.test.tsx:37-42` asserts Reading click is a no-op.
2. **"The Upload screen renders: page header ('Add a book' eyebrow + 'Upload an EPUB.' title + tagline), the Dropzone component in its idle state, and an empty area below reserved for the pipeline panel."** — PASS. `UploadScreen.tsx:180-218` renders the eyebrow, h1, tagline, and a `Dropzone` whose `state={dropzoneState(phase)}` resolves to `"idle"` when `phase.kind === "idle"`. The pipeline panel is gated by `phase.kind === "tracking"` (`UploadScreen.tsx:220`), so nothing renders below Dropzone in idle. `UploadScreen.test.tsx:59-65` asserts all three header strings plus the idle Dropzone copy.
3. **"Dragging a valid .epub onto the Dropzone updates its visual state to hover; dropping it OR selecting a file via the hidden file input, POSTs the file as multipart/form-data to http://localhost:8000/books/upload and the UI transitions to uploading."** — PASS. `Dropzone.tsx:57-81` implements `onDragOver`/`onDragLeave` (setting `dragOver` which drives `isHover`), `onDrop` (fires `onFile`), and a hidden `<input type="file">` (`Dropzone.tsx:156-163`). `UploadScreen.tsx:162-165` transitions phase to `"uploading"` on `handleFile`, and the effect at `:86-115` calls `uploadBook(file)`. `api.ts:61-84` sends `POST http://localhost:8000/books/upload` with `FormData`. `api.test.ts:58-78` asserts URL, method, `body instanceof FormData`, and `form.get("file") instanceof File`.
4. **"On a successful upload response ({ book_id, message }), the Dropzone shows done with the filename, and a pipeline panel appears below showing the book's book_id (as a monospace subtitle) and 7 PipelineRows labelled in this order: Parse EPUB, Run BookNLP, Resolve coref, Discover ontology, Review ontology, Cognee batches, Validate."** — PASS. `UploadScreen.tsx:19-27` declares `STAGE_DISPLAY` in exactly that order; `:249-270` renders the `phase.book_id` in `fontFamily: "var(--mono)"` followed by `.map` over `STAGE_DISPLAY`. `dropzoneState(phase)` returns `"done"` when `phase.kind === "tracking"` (`:63-70`). `UploadScreen.test.tsx:108-121` asserts the row labels match the array literal in exact order — this was one of the early-review fixes.
5. **"Immediately after the upload resolves, the frontend begins polling GET …/status every 2000 ms. Each poll updates every PipelineRow's state badge from the corresponding entry in stages using the mapping in Data contracts."** — PASS. `UploadScreen.tsx:144` uses `window.setInterval(..., 2000)` wrapping `tick()`. `:35-43` defines `badgeFor` with the exact PRD mapping (`pending→idle`, `running→running`, `complete→done`, `failed→error`). `UploadScreen.test.tsx:98-105` advances timers by 6000 ms and asserts `statusSpy.mock.calls.length` is within `[3, 4]`, rejecting both overly-fast and overly-slow cadences — this was early-review fix #1.
6. **"Polling stops as soon as the response has ready_for_query: true OR any stage has status: 'failed'. No further network calls to /status are made after that."** — PASS. `UploadScreen.tsx:132-137` flips `stopPolling.current = true` when either condition holds; `:144-149` clears the interval on the next tick. `UploadScreen.test.tsx:173-177` (ready branch) and `:211-215` (failed branch) both snapshot `statusSpy.mock.calls.length` then advance 4 s and assert `.toBe(callsAtReady/callsAtFail)`.
7. **"When ready_for_query becomes true, a 'Back to Library' action is rendered; clicking it navigates to / and the Library re-fetches GET /books so the new book is visible in the grid."** — PASS. `UploadScreen.tsx:273-285` renders `<Link to="/"><Button variant="primary">Back to Library</Button></Link>` gated by `ready`. `LibraryScreen.tsx:20-37` uses `useEffect(..., [pathname])` to refetch on route changes. `LibraryScreen.test.tsx:76-109` is the restructured single-router `<Link>` harness that does a real back-and-forth between `/` and `/upload`, asserting `fetchMock` is called twice — this was early-review fix #2.
8. **"If any stage returns status: 'failed', the corresponding PipelineRow renders with state='error' and shows the sanitized error string as the row's meta or as a secondary line; polling halts; and an inline error banner is rendered above the pipeline panel. Other stages retain their last-known state."** — PASS. `metaFor` (`UploadScreen.tsx:45-54`) returns `stage.error` for failed stages; `firstFailedStage` (`:72-79`) drives a `<div role="alert">` banner above the panel (`:222-238`). `UploadScreen.test.tsx:204-209` asserts `role="alert"` contains "pipeline failed", "OOM killed" is visible, AND `"0.4s"` (the parse_epub completion meta) survives the failure — the last assertion is early-review fix #5.
9. **"Upload errors surface in-place without crashing the screen: HTTP 400 shows 'Only .epub files are accepted'; 413 shows 'File too large (max 500 MB)'; 429 shows 'Too many pipelines running, try again later'; any other non-2xx shows the backend detail text. The Dropzone enters error state displaying the mapped message (an explicit error visual tied to the user's action) and the user can drop or browse again to retry."** — PASS. `api.ts:86-92` maps each status exactly. `api.test.ts:80-157` has exact-string assertions for 400/413/429, a backend-detail fallback, and a JSON-parse-failure branch. `UploadScreen.tsx:101-110` routes the error into `phase.kind === "error"` which `dropzoneState` returns as `"error"` (`:67`), and `Dropzone.tsx:134-137` renders `<div role="alert">{errorMessage}</div>` inside the zone itself. `UploadScreen.test.tsx:218-234` rejects with a 400 `UploadError` and asserts the alert text. The PRD was amended pre-generation — line 28 now reads "Dropzone enters `error` state displaying the mapped message" (early-review fix #3).
10. **"npm run test and pytest -v both pass. No existing tests regress. No new backend endpoints are introduced."** — PASS. 906/906 pytest pass (unchanged from slice-1 final), 56/56 Vitest pass, 4/4 Playwright pass. `git diff 6e5ff99..HEAD --name-only` shows zero changes to `main.py`, `models/`, `pipeline/`, or any backend module.

## Early-review fix verification

1. **Tighten polling cadence (UploadScreen.test.tsx advances 6000 ms, asserts 3–4 calls; implementation uses `setInterval(…, 2000)`).** DONE. `UploadScreen.tsx:144` reads `window.setInterval(() => { ... }, 2000)`. Test at `:98-105` advances 6000 ms and asserts `>= 3 && <= 4`.
2. **Row order assertion with inline order array.** DONE. `UploadScreen.test.tsx:108-121` collects `screen.getAllByText(/^(Parse EPUB|…|Validate)$/)` and `toEqual([...])` against the literal 7-element array in spec order.
3. **"0.4s duration survives failure" assertion.** DONE. `UploadScreen.test.tsx:209` contains `expect(screen.getByText("0.4s")).toBeInTheDocument()` after the failed-banner assertions.
4. **LibraryScreen refetch uses single MemoryRouter + Link harness AND `useEffect(() => fetchBooks(), [pathname])`.** DONE. `LibraryScreen.tsx:37` closes `useEffect(..., [pathname])`. `LibraryScreen.test.tsx:76-109` uses a single `<MemoryRouter>` with `<Routes>` and two `<Link>`s, clicking back and forth, asserting `toHaveBeenCalledTimes(2)`. An implementation with `[]` deps would fail this test.
5. **PRD AC 9 amendment reads "Dropzone enters error state" not "returns to idle".** DONE. Current `docs/superpowers/specs/2026-04-21-slice-2-upload-pipeline-status.md:28` ends with "The Dropzone enters `error` state displaying the mapped message (an explicit error visual tied to the user's action) and the user can drop or browse again to retry." The pre-amendment version (verified via `git show 6e5ff99:…`) read "The Dropzone returns to `idle`." Both implementation and tests match the amended wording.

## Findings

None that warrant REVISE. Noted for future housekeeping, not blocking:

- The Generator folded a frontend `.gitignore` update into the Task 7 Playwright commit (`8c7f4dc`) rather than a standalone chore commit. Cosmetic; the rubric explicitly excludes this from REVISE criteria.
- One React `act()` advisory warning in the Vitest log stems from the polling `setInterval` firing a `setState` during teardown of the `"uploads the dropped file…"` test. The test still passes deterministically and the advisory does not affect production behavior.
- `.env.example` documents no new env vars — correct, because none were added. If the backend `BASE_URL` is ever made configurable (e.g. for deployment), introduce `VITE_API_BASE` at that point.

## Summary

Slice 2 ships the Upload screen end-to-end — react-router-dom wiring, a drag-and-drop Dropzone, real `POST /books/upload` + 2 s `GET /status` polling with 7 stage rows, pipeline failure banner, and a Library refetch on route entry — with 906 pytest / 56 Vitest / 4 Playwright tests all green and zero backend or CORS changes.
