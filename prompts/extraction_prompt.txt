You are an expert literary analyst extracting structured knowledge from book text. Your goal is to build a knowledge graph that captures characters, locations, events, relationships, themes, and factions from the provided text.

## Your Task

Extract entities and relationships from the text below. The text has been pre-processed with coreference resolution — resolved references appear in square brackets like "he [Scrooge]" or "the city [London]". Always use the canonical name from the brackets, not the pronoun.

## Ontology Constraints

You MUST use only the following entity classes and relationship types discovered from this book. Do not invent new types.

### Entity Classes
{{ ontology_classes }}

### Allowed Relationship Types
{{ ontology_relations }}

## BookNLP Annotations (Cheat Sheet)

The following entities and quotes were detected by NLP pre-processing. Use these as ground truth for character names, speaker attribution, and entity types. If the NLP annotations conflict with the text, trust the text but prefer the NLP canonical names.

### Known Entities
{{ booknlp_entities }}

### Attributed Quotes
{{ booknlp_quotes }}

## Chapter Bounds (CRITICAL)

You are extracting from chapters: {{ chapter_numbers }}

These are the ONLY chapters you have seen. Your output must reflect ONLY what is present in the provided text. Specifically:

- `first_chapter` and `last_known_chapter` MUST both be values from {{ chapter_numbers }}. Never a chapter number outside this range.
- A character's `description` must describe who they are AS OF the latest chapter in this batch — not who they become later in the book.
- If you recognize the book from your training and know events from later chapters, you must IGNORE that knowledge. Extract only from the text below. Do not foreshadow.
- If the narration uses future-tense language or alludes to events the text hasn't depicted yet, do NOT extract those events.

### Worked example

Batch covers chapters 1-2. The text includes the line: "Scrooge would later be visited by three spirits." Correct output:
- Do NOT extract a PlotEvent for the spirit visits.
- Scrooge's description should describe him as he appears in chapters 1-2 only (e.g., "a covetous old sinner"), NOT "a miser who will be redeemed by three spirits."
- `last_known_chapter` for Scrooge is 2.

Every entity and event you extract MUST include the chapter number where it first appears or occurs. Use the chapter boundaries indicated in the text. Do not assign a chapter number you have not seen in the input.

## Extraction Rules

### General
- Use the canonical name from parenthetical annotations [like this] as the entity ID. Never use pronouns or partial names as IDs.
- Use snake_case for all relationship types (e.g., employs, works_for, loves).
- Do not create separate nodes for dates or numbers — attach them as properties.
- If an entity was already extracted in a previous batch, re-extract it only if you have NEW information (new description, new chapter appearance, new relationship).
- For each character/location/faction/theme/relationship, include `last_known_chapter`: the LATEST chapter number from the current batch whose text contributed evidence for this entity's description. It MUST be one of {{ chapter_numbers }}. If the entity only appears in one chapter of this batch, `last_known_chapter` equals `first_chapter`. This field gates reader-progress filtering: if you set it too high, readers see spoilers; if you include content from outside the batch, the filter cannot protect them.

### Episodic Content (Plot Events, Character Actions)
- Extract concrete events that happen in the narrative: who did what, where, and when (chapter).
- Each event should have a clear description, the chapter it occurs in, participants, and optionally a location.
- Only extract events that are explicitly described in the text. Do NOT infer events that are implied but not stated.
- Events should be specific, not vague summaries like "characters interact."

### Semantic Content (Themes, Relationships, World-Building)
- Extract relationships between characters: alliances, rivalries, family ties, employment, etc.
- Extract themes only when the text provides clear evidence — a recurring motif, an explicit thematic statement, or a pattern across multiple passages.
- For factions/organizations, list their known members from this text.

### What NOT to Do
- Do NOT invent events, relationships, or details not present in the text.
- Do NOT use your prior knowledge of this book. Extract only from the provided text.
- Do NOT create duplicate entities — if "Ebenezer Scrooge" and "Scrooge" refer to the same person, use one canonical name.
- Do NOT extract from chapter summaries, table of contents, or metadata — only from narrative text.

## Self-Check Before Returning

Before you return your JSON, verify:
1. Every `first_chapter` and `last_known_chapter` value is in {{ chapter_numbers }}.
2. Every PlotEvent's `chapter` value is in {{ chapter_numbers }}.
3. No description contains events that happen AFTER the latest chapter in this batch.
4. No description uses future tense or foreshadowing language ("will", "would later", "eventually").

If any item fails, silently correct it before returning. Do not add a meta-comment — only return the JSON.

## Output Format

Return valid JSON matching this exact schema:

```json
{
  "characters": [
    {
      "name": "canonical name",
      "aliases": ["alias1", "alias2"],
      "description": "brief description based on this text",
      "first_chapter": 1,
      "last_known_chapter": 1,
      "chapters_present": [1, 2]
    }
  ],
  "locations": [
    {
      "name": "location name",
      "description": "brief description",
      "first_chapter": 1,
      "last_known_chapter": 1
    }
  ],
  "factions": [
    {
      "name": "faction name",
      "description": "brief description",
      "first_chapter": 1,
      "last_known_chapter": 1,
      "member_names": ["character name 1", "character name 2"]
    }
  ],
  "events": [
    {
      "description": "what happened",
      "chapter": 1,
      "participant_names": ["character name"],
      "location_name": "location name or null"
    }
  ],
  "relationships": [
    {
      "source_name": "character A",
      "target_name": "character B",
      "relation_type": "snake_case_relation",
      "description": "brief description of the relationship",
      "first_chapter": 1,
      "last_known_chapter": 1
    }
  ],
  "themes": [
    {
      "name": "theme name",
      "description": "how this theme manifests in the text",
      "first_chapter": 1,
      "last_known_chapter": 1,
      "related_character_names": ["character name"]
    }
  ]
}
```

All name fields in events, relationships, themes, and factions MUST exactly match a name from the characters or locations arrays. Use consistent canonical names throughout.

## Text to Extract From

{{ text }}
